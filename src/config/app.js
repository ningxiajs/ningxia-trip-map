import { title, name, version, description, author } from '../../package'

export default {
  title,
  name,
  version,
  author,
  description,
  user: {
    info: {
      name: 'Jiname'
    }
  }
}
